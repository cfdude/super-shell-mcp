# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    default: {}
  commandFunction: |
    (config) => ({
      command: 'python',
      args: ['-m', 'your_pkg']
    })
  exampleConfig: {}
